{
  "scripts": {
    "build": "npx cap copy",
    "sync": "npx cap sync",
    "start": "npx cap serve",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.2",
    "@capacitor/ios": "^6.2.0"
  }
}
